package test;

import vni.String;

object TestNode {
	
	int level = -1;
	String instance = "blubb";
	
	void handle(TestType ev);
	
	Value test_func(Value val) const;
	value_t test_func2(value_t val) const;
	
}
