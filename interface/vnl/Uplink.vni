package vnl;

object Uplink {
	
	int error_interval = 3000;
	
	void reset();
	void shutdown();
	int get_fd() const;
	
	void publish(Topic topic);
	void subscribe(Topic topic);
	
	void forward(long domain, long topic);
	
}
